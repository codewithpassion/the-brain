export const meta = {
  name: 'brain-platform-loop7',
  description: 'Invariant sweep: enforce trust_grade-is-sidecar-only and brain-entities-in-every-env everywhere; align api_keys.allowed_scopes; rebuild; re-score',
  phases: [{ title: 'Sweep' }, { title: 'Rebuild' }, { title: 'Rescore' }],
}
const j = (o) => JSON.stringify(o)
const BASE = '/tmp/claude-1000/-home-roboto-devel/de8de32f-fcde-4884-89d9-eeaaf43cb4da/scratchpad'
const PRD_PATH = `${BASE}/PRD.md`
const SECTIONS = { s01:'1. Vision', s02:'2. Architecture', s03:'3. Data Model', s04:'4. Ingestion', s05:'5. Search', s06:'6. Graph', s07:'7. Tenancy', s08:'8. Sessions/Backfill', s09:'9. API/MCP/CLI/Frontend', s10:'10. Ops', appendix:'Appendix' }
const SECTION_KEYS = Object.keys(SECTIONS)
const INTEGRATE_SCHEMA = { type:'object', required:['filesEdited','changelog','residual'], additionalProperties:false, properties:{ filesEdited:{type:'array',items:{type:'string'}}, changelog:{type:'array',items:{type:'string'}}, residual:{type:'array',items:{type:'string'}} } }
const REVIEW_SCHEMA = { type:'object', required:['lens','score','verdict','weaknesses','summary'], additionalProperties:false, properties:{ lens:{type:'string'}, score:{type:'number'}, verdict:{type:'string',enum:['accept','accept-with-changes','major-revision','reject']}, strengths:{type:'array',items:{type:'string'}}, weaknesses:{type:'array',items:{type:'object',required:['section','issue','severity','recommendation'],additionalProperties:false,properties:{section:{type:'string',enum:SECTION_KEYS},issue:{type:'string'},severity:{type:'string',enum:['critical','high','medium','low']},recommendation:{type:'string'}}}}, missing:{type:'array',items:{type:'string'}}, summary:{type:'string'} } }

const INV = `INVARIANT (canonical, from §3.2/§5.2 — already correct there): trust_grade / trustGrade is an agent-behaviour signal that lives ONLY in the memory_use_policy sidecar. It is NEVER a Vectorize metadata index, NEVER a Vectorize filter field, NEVER part of any Vectorize upsert-metadata blob, NEVER in a "rides as filterable metadata" list, and NEVER in the list of Vectorize-returned fields that the post-query D1 re-check re-checks. It is applied ONLY as a post-hydration LEFT-JOIN boost on the memory_use_policy row. Every violation must be removed; the legitimate sidecar JOIN/boost stays.`
const RULES = `RULES: consistency/invariant sweep — no new design. Edit in place (Write/Edit), same path. Preserve headings/structure. Do NOT add/remove "#### Code references" blocks. After editing, GREP-VERIFY the invariant holds in your file(s). Return the structured object.`

phase('Sweep')
const sweep = (await parallel([
  () => agent(`INVARIANT SWEEP — remove trust_grade from the VECTOR PATH. EDIT ONLY: ${BASE}/s05.md and ${BASE}/s08.md.\n\n${INV}\n\nPUNCH-LIST:\n1) s05 §5.3.2: the vectorSearch filter param TYPE includes \`trust_grade?: string\` (~line 1781) and a "metadata" list names trust_grade as filterable (~line 1767). REMOVE trust_grade from the filter type AND the filterable-metadata list. (Keep the post-hydration trust-grade boost on the memory_use_policy LEFT-JOIN, which is correct.)\n2) s08 §8.3 (~line 3571): the session-chunk Vectorize upsert metadata blob is \`{userId, teamId, scope, sessionId, visibility, trust_grade:'evidence', embedding_model}\`. REMOVE \`trust_grade:'evidence'\` from the Vectorize upsert metadata (the session fact's grade is recorded in memory_use_policy, not in the vector). Keep all other fields.\nGrep-verify: no trust_grade/trustGrade survives in any Vectorize upsert/filter/metadata context in these two files. ${RULES}`,
    { label:'sweep:vectorpath', phase:'Sweep', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
  () => agent(`INVARIANT SWEEP — Ops provisioning + per-env bindings. EDIT ONLY: ${BASE}/s10.md.\n\n${INV}\n\nPUNCH-LIST:\n1) §10.4 (~line 4181): DELETE the line \`wrangler vectorize create-metadata-index brain-chunks --property-name=trust_grade --type=string\`. The brain-chunks indexed set is exactly scope, team_id, visibility, embedding_model.\n2) §10.6 deploy checklist AND §10.7 runbook step 3 (~line 4293): remove trust_grade from the brain-chunks metadata-index list wherever it appears (→ team_id/scope/visibility/embedding_model).\n3) §10.2 environment overrides (~lines 4131-4132): the staging AND production \`vectorize\` arrays declare only brain-chunks(-staging). ADD the second binding {binding:'ENTITY_INDEX', index_name:'brain-entities(-staging)' / 'brain-entities'} to BOTH the staging and production env blocks so the entity index exists per-environment.\n4) §10.7 (~line 4325): the rollout-gate example "a risky change (e.g. Phase 3.5 entity vectors)" is stale — entity vectors are v1. Change the example to "(e.g. Phase 3.5 semantic fact dedup)".\nGrep-verify: zero trust_grade in any §10 provisioning/checklist; brain-entities present in every env vectorize block. ${RULES}`,
    { label:'sweep:ops', phase:'Sweep', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
  () => agent(`INVARIANT SWEEP — prose invariant + api_keys.allowed_scopes alignment. EDIT ONLY: ${BASE}/s03.md, ${BASE}/s07.md, ${BASE}/s09.md.\n\n${INV}\n\nPUNCH-LIST:\n1) s07 §7.4 (~line 3110 in the assembled doc): prose states "Within the namespace, userId/teamId/scope/visibility/trust_grade/embedding_model ride as filterable metadata." REMOVE trust_grade from that filterable-metadata list. Nearby (~line 3121): the post-query D1 re-check field list "tenant_id/scope/trust_grade/deleted_at" — remove trust_grade from the list of Vectorize-returned fields being re-checked (the trust grade comes from the memory_use_policy JOIN as a boost, not from a Vectorize-returned field). Keep that sidecar JOIN/boost intact. DO NOT alter §3.2 (it is the canonical-correct source).\n2) api_keys.allowed_scopes PHANTOM-COLUMN alignment: §9.0.1 resolves the api-key branch's allowedScopes from \`api_keys.allowed_scopes\`, but §7.2's mint-time derivation and the §7.1 / §3.1.4 \`api_keys\` DDL may not define that exact column. ALIGN to ONE canonical column: ensure \`api_keys.allowed_scopes\` (TEXT/JSON, nullable; NULL = unrestricted) is (a) DEFINED in the api_keys DDL in BOTH §7.1 (s07) and §3.1.4 (s03), (b) WRITTEN at mint in §7.2 as the intersection of requested scopes with the minter's grant, and (c) READ in §9.0.1 (s09) exactly as written. Fix whichever of the three sides is inconsistent so the column name + semantics match end-to-end; a restricted key must never resolve to '*'.\nGrep-verify: trust_grade gone from §7.4 filterable/re-check lists; api_keys.allowed_scopes defined+written+read consistently across s03/s07/s09. ${RULES}`,
    { label:'sweep:prose-apikeys', phase:'Sweep', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
])).filter(Boolean)
log(`Invariant sweep: ${sweep.reduce((a,i)=>a+(i.changelog?i.changelog.length:0),0)} edits across ${sweep.flatMap(i=>i.filesEdited||[]).length} files.`)

phase('Rebuild')
const rebuilt = await agent(`Run with Bash and report stdout verbatim:\n\ncd ${BASE} && python3 build.py\n\nThen report ${PRD_PATH} byte size (wc -c). Return only that.`, { label:'rebuild:prd', phase:'Rebuild', agentType:'general-purpose', model:'sonnet', effort:'low' })
log(`Rebuilt: ${rebuilt}`)

phase('Rescore')
const LENSES = [
  { key:'cf-correctness', prompt:'Cloudflare-primitive correctness within real V2 limits. SPECIAL FOCUS: trust_grade must NOT appear as a Vectorize metadata index/filter/upsert-field anywhere (§3.2/§5.2 canonical); verify §5.3.2, §7.4, §8.3, §10.4, §10.6, §10.7 all comply. Verify brain-entities is declared in every §10.1/§10.2 env binding block and provisioned in §10.4.' },
  { key:'completeness', prompt:'Requirements coverage & internal consistency — verify no surviving contradictions on trust_grade location or brain-entities provisioning, and that api_keys.allowed_scopes is defined+written+read consistently across §3.1.4/§7.1/§7.2/§9.0.1.' },
  { key:'security-isolation', prompt:'Multi-tenant + intra-tenant isolation. Verify: api_keys.allowed_scopes is a real defined column written at mint and read at resolve (no phantom column; restricted key never resolves to "*"); trust_grade is not a Vectorize filter (no cross-grade leak via metadata); admin break-glass remains fail-closed/audited; entity search arm carries the same tenant+scope/visibility gate as the chunk arm.' },
]
const reviews = (await parallel(LENSES.map((lens)=>()=>agent(
  `Ruthless adversarial re-score of the Brain Platform PRD (Cloudflare-native multi-tenant memory platform) AFTER an invariant sweep (trust_grade sidecar-only enforced everywhere; brain-entities in every env block; api_keys.allowed_scopes aligned). Prior aggregate progression: 26,68,61,62,57,68.\n\nYOUR LENS: ${lens.prompt}\n\nRead the FULL current PRD at ${PRD_PATH}. You MAY read /home/roboto/devel/{cf-graph,gbrain-cf,openbrains}. Section keys: ${j(SECTIONS)}. Score 0-100 (any unresolved critical caps below 60). Tag each weakness with section key + severity + concrete fix. Set lens to "${lens.key}". Return the structured review.`,
  { label:`rescore:${lens.key}`, phase:'Rescore', schema:REVIEW_SCHEMA, agentType:'general-purpose', effort:'high' })))).filter(Boolean)
const scores = reviews.map(r=>r.score||0)
const aggregate = scores.length ? Math.round(scores.reduce((a,b)=>a+b,0)/scores.length) : 0
log(`Re-score: ${reviews.map(r=>`${r.lens}=${r.score}`).join(', ')} -> aggregate ${aggregate}/100`)

return { iteration:7, priorProgression:[26,68,61,62,57,68], aggregateScore:aggregate, sweepApplied:sweep,
  reviews: reviews.map(r=>({ lens:r.lens, score:r.score, verdict:r.verdict, strengths:r.strengths||[], weaknesses:r.weaknesses||[], missing:r.missing||[], summary:r.summary })) }
