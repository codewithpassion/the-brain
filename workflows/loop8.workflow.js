export const meta = {
  name: 'brain-platform-loop8',
  description: 'Integrate confirmed deployment facts (1-2 tenants / Workers Paid / Clerk / $400-mo) + simplify scale-out to Phase-N + clear R7 debris, rebuild, re-score',
  phases: [{ title: 'Apply' }, { title: 'Rebuild' }, { title: 'Rescore' }],
}
const j = (o) => JSON.stringify(o)
const BASE = '/tmp/claude-1000/-home-roboto-devel/de8de32f-fcde-4884-89d9-eeaaf43cb4da/scratchpad'
const PRD_PATH = `${BASE}/PRD.md`
const SECTIONS = { s01:'1. Vision', s02:'2. Architecture', s03:'3. Data Model', s04:'4. Ingestion', s05:'5. Search', s06:'6. Graph', s07:'7. Tenancy', s08:'8. Sessions/Backfill', s09:'9. API/MCP/CLI/Frontend', s10:'10. Ops', appendix:'Appendix' }
const SECTION_KEYS = Object.keys(SECTIONS)
const INTEGRATE_SCHEMA = { type:'object', required:['filesEdited','changelog','residual'], additionalProperties:false, properties:{ filesEdited:{type:'array',items:{type:'string'}}, changelog:{type:'array',items:{type:'string'}}, residual:{type:'array',items:{type:'string'}} } }
const REVIEW_SCHEMA = { type:'object', required:['lens','score','verdict','weaknesses','summary'], additionalProperties:false, properties:{ lens:{type:'string'}, score:{type:'number'}, verdict:{type:'string',enum:['accept','accept-with-changes','major-revision','reject']}, strengths:{type:'array',items:{type:'string'}}, weaknesses:{type:'array',items:{type:'object',required:['section','issue','severity','recommendation'],additionalProperties:false,properties:{section:{type:'string',enum:SECTION_KEYS},issue:{type:'string'},severity:{type:'string',enum:['critical','high','medium','low']},recommendation:{type:'string'}}}}, missing:{type:'array',items:{type:'string'}}, summary:{type:'string'} } }

const FACTS = `CONFIRMED DEPLOYMENT FACTS (integrate as DECIDED values; resolve the matching "pending confirmation" residuals):
- SCALE (v1): 1-2 tenants only, internal dogfood for the owner's new service company; vector/session counts unknown but SMALL. Keep the multi-tenant ISOLATION model fully intact, but DEFER the horizontal SCALE-OUT machinery: v1 runs a SINGLE D1 database (no shard fan-out); the tenant_shards columns stay for forward-compat but resolve to one shard in v1; the Vectorize per-index ceilings (10M vectors / 50k namespaces) are explicitly NON-BINDING at 1-2 tenants. Frame D1 sharding + the shard-split-QPS threshold as a documented Phase-N scale-out TRIGGER, not a v1 build. This REMOVES hedging/complexity — do NOT add new sharding logic.
- PLATFORM PLAN: Cloudflare Workers PAID ($5/mo). Workers AI billed by Neurons (Paid includes ~10,000 Neurons/day, then ~$0.011/1k Neurons). Make INFORMED, explicitly-LABELED assumptions for per-model request rate limits (state assumed conservative values; note they are unverified-until-staging and NON-BINDING at 1-2 tenants), and document the upgrade path: AI Gateway (caching/retry/fallback) + bring-your-own / OpenAI-compatible models later via the AI provider abstraction (AI_PROVIDER=openai-compatible, as in cf-graph apps/graph/src/api/services/factory.ts). Locked models stay: bge-m3 (embed), llama-3.3-70b-instruct-fp8-fast (synthesis, 24k ctx), bge-reranker-base (rerank).
- AUTH / IdP: Clerk (OAuth 2.1 upstream IdP) — CONFIRMED; matches gbrain-cf + openbrains. Remove any "IdP TBD / TBD provider" hedging.
- COST CEILING: MONTHLY_COST_CEILING_USD = 400, CONFIGURABLE via env var (per-tenant-overridable). The token_spend 429 enforcer trips when projected monthly spend would exceed it; at 1-2 tenants this is effectively the whole-platform budget.`
const RULES = `RULES: integrate decided facts + clear the named debris; no speculative new features. Edit files in place (Write/Edit), same path. Preserve headings/structure. Do NOT add/remove "#### Code references" blocks. Keep shared names consistent (ScopedDB, brain-chunks, brain-entities, memory_use_policy, tenant_shards, api_keys.allowed_scopes, MONTHLY_COST_CEILING_USD). After editing, grep-verify your changes. Return the structured object.`

phase('Apply')
const apply = (await parallel([
  () => agent(`Integrate deployment facts + clear one debris item. EDIT ONLY: ${BASE}/s01.md and ${BASE}/s02.md.\n\n${FACTS}\n\nTASKS:\n- s01: set the v1 DEPLOYMENT TARGET = 1-2 tenants (internal dogfood for the owner's new service company). Update personas / scope-boundary / success-criteria / non-goals so HORIZONTAL SCALE-OUT (D1 sharding) is an explicit deferred-to-Phase-N item, NOT an "unknown scale" hedge. Keep the multi-tenant architecture and isolation as v1.\n- s02: in the architecture, state v1 runs a SINGLE D1 (sharding is a documented future layer, not built), and make the AI-provider abstraction (AI Gateway + BYO/OpenAI-compatible models later) an explicit architectural seam. DEBRIS FIX: find the ~6 §2 code-reference/prose lines that still instruct moving trust_grade INTO Vectorize metadata and correct them to the canonical sidecar-only invariant (trust_grade lives ONLY in memory_use_policy, §3.2/§5.2; never a Vectorize index/filter/upsert field).\n${RULES}`,
    { label:'apply:scale-arch', phase:'Apply', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
  () => agent(`Integrate deployment facts into data-model + ops. EDIT ONLY: ${BASE}/s03.md and ${BASE}/s10.md.\n\n${FACTS}\n\nTASKS:\n- s03: mark tenant_shards as SINGLE-SHARD in v1 (columns retained for forward-compat; horizontal split deferred to Phase-N). Annotate the Vectorize 10M-vector / 50k-namespace ceilings as NON-BINDING at 1-2 tenants. Remove/resolve the "shard-split QPS threshold needs tuning" residual (v1 = one shard; split is a future trigger).\n- s10: (a) add a 'Plan, models & rate limits' note — Workers Paid ($5/mo), Workers AI Neuron budget (~10k/day incl.), INFORMED LABELED per-model rate-limit assumptions (conservative; unverified-until-staging; non-binding at 1-2 tenants), AI Gateway, and the BYO/OpenAI-compatible upgrade path; (b) set MONTHLY_COST_CEILING_USD=400 (configurable env var, per-tenant-overridable) and wire the token_spend 429 enforcer to it; (c) add Clerk env/secrets (CLERK_* / OAuth) to the bindings/secrets; (d) since v1 is single-D1, ensure the deploy/provisioning describes ONE D1 (no shard fan-out) while keeping the TWO Vectorize indexes (brain-chunks + brain-entities).\n${RULES}`,
    { label:'apply:data-ops', phase:'Apply', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
  () => agent(`Integrate facts into search/tenancy + clear three debris items. EDIT ONLY: ${BASE}/s05.md and ${BASE}/s07.md.\n\n${FACTS}\n\nTASKS:\n- s05: where the AI models / embedding / rerank are described, reference the rate-limit ASSUMPTIONS + Neuron budget + BYO upgrade path consistently (point to §10 for detail; do not duplicate the whole table).\n- s07: (a) state Clerk is the confirmed OAuth 2.1 upstream IdP in the auth/identity model (remove any TBD-provider hedge). DEBRIS FIXES: (b) foldPartitionFilter (§7.3) currently folds only a SINGLE scope — extend it to MULTI-scope using {scope:{\$in:[...granted scopes]}} (the form already agreed in §5.3.2), so a multi-grant principal gets one filtered query, not a single-scope-only filter; (c) the link-arm getLinks/expandFrontier (§7.3) visibility/scope gating must apply the same node-gate as BFS to BOTH link endpoints (make it consistent with nodeGate/nodeVisible); (d) nodeGate/nodeVisible/resolveScopedNode build SQL by interpolating EdgeSpec field names — add a sentence that those field names are validated against a fixed allowlist of known column names (not arbitrary input) so it is not an injection surface.\n${RULES}`,
    { label:'apply:search-tenancy', phase:'Apply', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
  () => agent(`Clear the §9 isolation debris + update the decision log. EDIT ONLY: ${BASE}/s09.md and ${BASE}/appendix.md.\n\n${FACTS}\n\nTASKS:\n- s09: §9.0.1 membership-resolution SQL currently does 'SELECT tenant_id, team_id, role, allowed_scopes FROM memberships WHERE user_id = ?' with NO tenant predicate — a user who belongs to MULTIPLE tenants would over-match. FIX: scope the resolution to the request's tenant (WHERE user_id = ? AND tenant_id = ?), so the resolved Principal is bound to exactly one tenant per request (the tenant_id comes from the authenticated context / OAuth resource). Ensure downstream Principal construction is tenant-bound.\n- appendix: update the decision/residual log — mark RESOLVED with these values: (1) SCALE = 1-2 tenants v1, single D1, horizontal sharding deferred to Phase-N, Vectorize ceilings non-binding; (2) Workers PAID plan + informed rate-limit assumptions + AI Gateway + BYO/OpenAI-compatible upgrade path; (3) IdP = Clerk; (4) MONTHLY_COST_CEILING_USD = 400 (configurable). Keep as TRUE-UNKNOWN only the exact vector/session counts (note: 'measure during dogfood'). Remove now-resolved 'pending product sign-off / pending confirmation' items.\n${RULES}`,
    { label:'apply:api-appendix', phase:'Apply', schema:INTEGRATE_SCHEMA, agentType:'general-purpose', model:'opus', effort:'high' }),
])).filter(Boolean)
log(`Applied: ${apply.reduce((a,i)=>a+(i.changelog?i.changelog.length:0),0)} edits across ${apply.flatMap(i=>i.filesEdited||[]).length} files.`)

phase('Rebuild')
const rebuilt = await agent(`Run with Bash and report stdout verbatim:\n\ncd ${BASE} && python3 build.py\n\nThen report ${PRD_PATH} byte size (wc -c). Return only that.`, { label:'rebuild:prd', phase:'Rebuild', agentType:'general-purpose', model:'sonnet', effort:'low' })
log(`Rebuilt: ${rebuilt}`)

phase('Rescore')
const LENSES = [
  { key:'cf-correctness', prompt:'Cloudflare-primitive correctness within real limits, NOW scoped to the confirmed v1 target (1-2 tenants, Workers Paid). Reward the design for right-sizing (single D1, sharding deferred, ceilings non-binding) rather than over-engineering. Verify the Workers AI rate-limit assumptions are sane and labeled, AI Gateway + BYO path documented, and MONTHLY_COST_CEILING_USD=400 wired to the token_spend enforcer.' },
  { key:'completeness', prompt:'Requirements coverage & internal consistency. Verify the four deployment facts are integrated consistently (no surviving "unknown scale" / "IdP TBD" / "rate limits pending" hedges that contradict the now-decided values), the scale-out deferral is consistent everywhere sharding is mentioned, and the R7 debris items are cleared.' },
  { key:'security-isolation', prompt:'Multi-tenant + intra-tenant isolation. Verify: §9.0.1 membership resolution is now tenant-bound (WHERE user_id AND tenant_id); foldPartitionFilter handles multi-scope via $in without leaking ungranted scopes; link-arm getLinks/expandFrontier gates both endpoints; trust_grade is nowhere in the vector path; admin break-glass remains fail-closed/audited; isolation is intact despite the single-D1 simplification (row-scoping + Vectorize namespace still enforced).' },
]
const reviews = (await parallel(LENSES.map((lens)=>()=>agent(
  `Ruthless adversarial re-score of the Brain Platform PRD AFTER integrating four confirmed deployment facts (1-2 tenants v1 / Workers Paid / Clerk / \$400-mo configurable) with scale-out deferred to Phase-N, plus clearing the round-7 debris. Prior aggregate progression: 26,68,61,62,57,68,67.\n\nYOUR LENS: ${lens.prompt}\n\nRead the FULL current PRD at ${PRD_PATH}. You MAY read /home/roboto/devel/{cf-graph,gbrain-cf,openbrains}. Section keys: ${j(SECTIONS)}. Score 0-100 (any unresolved critical caps below 60); judge against the CONFIRMED v1 target, not a hypothetical hyperscale deployment. Tag each weakness with section key + severity + concrete fix. Set lens to "${lens.key}". Return the structured review.`,
  { label:`rescore:${lens.key}`, phase:'Rescore', schema:REVIEW_SCHEMA, agentType:'general-purpose', effort:'high' })))).filter(Boolean)
const scores = reviews.map(r=>r.score||0)
const aggregate = scores.length ? Math.round(scores.reduce((a,b)=>a+b,0)/scores.length) : 0
log(`Re-score: ${reviews.map(r=>`${r.lens}=${r.score}`).join(', ')} -> aggregate ${aggregate}/100`)
return { iteration:8, priorProgression:[26,68,61,62,57,68,67], aggregateScore:aggregate, applyApplied:apply, reviews: reviews.map(r=>({ lens:r.lens, score:r.score, verdict:r.verdict, strengths:r.strengths||[], weaknesses:r.weaknesses||[], missing:r.missing||[], summary:r.summary })) }
