{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "the-brain strict base",
  "comment": "Thin re-export of the single source of truth in `packages/tsconfig/base.json` (see the note there). Kept so existing `../../tsconfig.base.json` references stay valid.",
  "extends": "./packages/tsconfig/base.json"
}
